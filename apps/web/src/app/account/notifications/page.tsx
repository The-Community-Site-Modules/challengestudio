// Route: /account/notifications — Email notification preferences
// NOTE: Workspace-scoped unsubscribe only — never affects other workspaces (PRD §15.2)
// TODO: Milestone 13
export default function NotificationsPage() {
  return <div className="p-8 text-muted-foreground">Notification Preferences — Milestone 13</div>
}
